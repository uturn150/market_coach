# Secrets and credentials only — everything else (paper_state/, data/, reference_code/, logs/) is now tracked.
*.keys.json
kraken.keys.json
alert.keys.json
research.keys.json
webapp_password.txt
webapp_password_hash.txt
.kraken_nonce
*.nonce
.balance_cache.json

# Python / environment (not project data)
__pycache__/
*.pyc
.pytest_cache/
.venv/
reference_code/
